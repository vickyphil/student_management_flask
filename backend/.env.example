SECRET_KEY=change-this-in-production
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/student_management
FLASK_DEBUG=1

# Optional SMTP. If omitted, verification/reset links are printed to the Flask console.
SMTP_HOST=
SMTP_PORT=587
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_USE_TLS=1
MAIL_FROM=no-reply@example.com
